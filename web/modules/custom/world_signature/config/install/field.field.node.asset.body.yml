# Body field instance on the asset bundle. Free-form description —
# fit notes, context, what makes this asset right for its slot.
# The structured metadata lives in dedicated fields (source URL,
# license, polycount, etc.); body is for the prose around it.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.body
    - node.type.asset
  module:
    - text
id: node.asset.body
field_name: body
entity_type: node
bundle: asset
label: Description
description: 'Fit notes and prose context. Structured metadata lives in dedicated fields below.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  display_summary: false
  required_summary: false
  allowed_formats: {  }
field_type: text_with_summary
