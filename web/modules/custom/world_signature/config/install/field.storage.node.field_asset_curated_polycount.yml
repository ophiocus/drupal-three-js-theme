# Curated asset: triangle count after decimation / cleanup. Should
# fall within the per-slot poly budget declared in
# docs/atmospheres/<name>/assets-needed.yml.
langcode: en
status: true
dependencies:
  module:
    - node
id: node.field_asset_curated_polycount
field_name: field_asset_curated_polycount
entity_type: node
type: integer
settings:
  unsigned: true
  size: normal
module: core
locked: false
cardinality: 1
translatable: false
indexes: {  }
persist_with_no_fields: false
custom_storage: false
