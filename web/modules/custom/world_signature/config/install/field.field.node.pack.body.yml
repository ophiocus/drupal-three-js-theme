# Body field instance on the pack bundle. Description / fit notes
# / overview of what the pack contains. Per-mesh notes live on the
# asset bundle.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.body
    - node.type.pack
  module:
    - text
id: node.pack.body
field_name: body
entity_type: node
bundle: pack
label: Description
description: 'Pack-wide description and overview. Per-mesh fit notes live on each asset child.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  display_summary: false
  required_summary: false
  allowed_formats: {  }
field_type: text_with_summary
