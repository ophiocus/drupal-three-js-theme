# Card view-display for profile. Body only — same shape as the
# /default view-display we ship for this bundle, just under the
# /card view-mode the FullView modal uses.
langcode: en
status: true
dependencies:
  config:
    - core.entity_view_mode.node.card
    - field.field.node.profile.body
    - field.field.node.profile.field_world_sector
    - field.field.node.profile.field_world_signature
    - node.type.profile
  module:
    - text
id: node.profile.card
targetEntityType: node
bundle: profile
mode: card
content:
  body:
    type: text_default
    label: hidden
    settings: {  }
    third_party_settings: {  }
    weight: 0
    region: content
hidden:
  field_world_sector: true
  field_world_signature: true
  langcode: true
  links: true
  search_api_excerpt: true
