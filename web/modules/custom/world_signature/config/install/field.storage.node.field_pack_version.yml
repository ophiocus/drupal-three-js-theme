# Pack version string as declared by the source — "v1.2", "1.0.3",
# "2024-08". When the source ships v1.3 with new meshes, create a
# new pack node (don't mutate the existing one) so provenance and
# child asset references stay intact.
langcode: en
status: true
dependencies:
  module:
    - node
id: node.field_pack_version
field_name: field_pack_version
entity_type: node
type: string
settings:
  max_length: 64
  case_sensitive: false
  is_ascii: false
module: core
locked: false
cardinality: 1
translatable: false
indexes: {  }
persist_with_no_fields: false
custom_storage: false
