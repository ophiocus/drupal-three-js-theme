# Card view-display for event. Body only — same shape as profile's
# card display. The user's roadmap item "showcase the dates" for
# events will eventually elevate the temporal anchor here (when
# field_event_date lands), but until that field exists the body
# carries the date in prose form.
langcode: en
status: true
dependencies:
  config:
    - core.entity_view_mode.node.card
    - field.field.node.event.body
    - field.field.node.event.field_world_sector
    - field.field.node.event.field_world_signature
    - node.type.event
  module:
    - text
id: node.event.card
targetEntityType: node
bundle: event
mode: card
content:
  body:
    type: text_default
    label: hidden
    settings: {  }
    third_party_settings: {  }
    weight: 0
    region: content
hidden:
  field_world_sector: true
  field_world_signature: true
  langcode: true
  links: true
  search_api_excerpt: true
