# slot instance on the asset bundle. Required — an asset without
# a slot can't be wired into the metaphor.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_asset_slot
    - node.type.asset
    - taxonomy.vocabulary.asset_slots
id: node.asset.field_asset_slot
field_name: field_asset_slot
entity_type: node
bundle: asset
label: 'Slot'
description: 'Which named role this asset fills in the metaphor. Atmosphere mappings.yml binds bundles to slots by name.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      asset_slots: asset_slots
    sort:
      field: name
      direction: asc
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
