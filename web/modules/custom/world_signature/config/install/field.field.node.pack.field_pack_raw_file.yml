# raw_file instance on the pack bundle. The pack as downloaded —
# any shape. Stored verbatim; never mutated.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_pack_raw_file
    - node.type.pack
  module:
    - file
id: node.pack.field_pack_raw_file
field_name: field_pack_raw_file
entity_type: node
bundle: pack
label: 'Raw file'
description: 'The pack as downloaded — .zip / .blend / .fbx / .obj / .glb / .gltf. Whatever shape received, preserved verbatim.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  file_directory: 'assets/packs/[date:custom:Y-m]'
  file_extensions: 'zip blend fbx obj glb gltf'
  max_filesize: ''
  description_field: false
  handler: 'default:file'
  handler_settings: {  }
field_type: file
