# The pack file as downloaded. Whatever shape received — .zip
# (multi-mesh pack), .glb / .gltf (single model), .blend (Blender
# source), .fbx / .obj. Preserved verbatim for provenance and so
# the curator can revisit the source bytes.
langcode: en
status: true
dependencies:
  module:
    - file
    - node
id: node.field_pack_raw_file
field_name: field_pack_raw_file
entity_type: node
type: file
settings:
  display_field: false
  display_default: false
  uri_scheme: public
  target_type: file
module: file
locked: false
cardinality: 1
translatable: false
indexes:
  target_id:
    - target_id
persist_with_no_fields: false
custom_storage: false
