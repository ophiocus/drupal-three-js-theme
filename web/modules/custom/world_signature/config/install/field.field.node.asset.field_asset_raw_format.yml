# raw_format instance on the asset bundle. Allowed values are in
# the storage (glb / gltf / fbx / obj / blend / zip).
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_asset_raw_format
    - node.type.asset
  module:
    - options
id: node.asset.field_asset_raw_format
field_name: field_asset_raw_format
entity_type: node
bundle: asset
label: 'Raw format'
description: 'File format as downloaded.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: list_string
