# license instance on the pack bundle. References the asset_licenses
# vocabulary; required because attribution and compatibility checks
# depend on knowing the tier.
langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_pack_license
    - node.type.pack
    - taxonomy.vocabulary.asset_licenses
id: node.pack.field_pack_license
field_name: field_pack_license
entity_type: node
bundle: pack
label: 'License'
description: 'License tier for the whole pack. Individual assets inherit it.'
required: true
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  handler: 'default:taxonomy_term'
  handler_settings:
    target_bundles:
      asset_licenses: asset_licenses
    sort:
      field: weight
      direction: asc
    auto_create: false
    auto_create_bundle: ''
field_type: entity_reference
