# Provenance: the license tier under which this asset was released.
# Entity-reference to a taxonomy term in the asset_licenses
# vocabulary (CC0, CC-BY, CC-BY-SA, CC-BY-NC, Sketchfab Standard,
# itch.io paid, Custom). Drives runtime attribution + per-property
# license-compatibility checks.
langcode: en
status: true
dependencies:
  module:
    - node
    - taxonomy
id: node.field_asset_license
field_name: field_asset_license
entity_type: node
type: entity_reference
settings:
  target_type: taxonomy_term
module: core
locked: false
cardinality: 1
translatable: false
indexes: {  }
persist_with_no_fields: false
custom_storage: false
